while (scanning)
    // {
    //     if (scanf("%c", &operations[last]) == EOF)
    //     {
    //         scanning = 0;
    //     }
    //     else
    //     {

    //         printf("?: %c", operations[last]);
    //         mats[last++] = mt_read();

    //         mats = realloc(mats, sizeof(matrix *) * (last + 1));

    //         last++;
    //     }
    // }