#include <stdio.h>

int main()
{
    /*
    int n = 0;
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            if (i == j)
                printf("%2d ", i*10 + j);
            else if (i+j == n-1)
                printf("%2d ", i*10 + j);
            else
                printf("   ");
        }
        printf("\n");
    }
    */
}